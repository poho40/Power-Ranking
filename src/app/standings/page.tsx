import type { Metadata } from "next";import { getDashboard } from "@/lib/data/getDashboard";import { PageHeader } from "@/components/layout/PageHeader";import { StandingsTable } from "@/components/standings/StandingsTable";import { ErrorState } from "@/components/ui/States";export const metadata:Metadata={title:"Standings"};export default async function Standings(){const {league,rankings,error}=await getDashboard();return <><PageHeader eyebrow="Actual results vs expectation" title="League Standings" description="Sort the table to find dominant offenses, fortunate schedules, and teams whose records undersell their play."/>{error||!league?<ErrorState message={error??"League unavailable"}/>:<StandingsTable league={league} rankings={rankings}/>}</>}
