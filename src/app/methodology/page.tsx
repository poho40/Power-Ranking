import type { Metadata } from "next";import { getLeague } from "@/lib/data/getLeague";import { generatePreseasonWeights } from "@/lib/preseason";import { PageHeader } from "@/components/layout/PageHeader";import { Card } from "@/components/ui/Card";import { ScoreBar } from "@/components/ui/ScoreBar";export const metadata:Metadata={title:"Methodology"};const metrics=[{name:"Scoring Strength",weight:30,text:"Points per game relative to every team in the league. It is the largest component because sustained scoring is the clearest signal of team quality."},{name:"Recent Form",weight:20,text:"The latest three completed scoring weeks, weighted 50%, 30%, and 20% from newest to oldest. This captures momentum without overreacting to one game."},{name:"Record Strength",weight:15,text:"Winning percentage, with ties worth half a win, normalized against the league."},{name:"Expected Wins",weight:15,text:"Each week is treated as an all-play round. Beating an opponent earns one comparison win; tying earns half. Weekly comparison wins are divided by the available opponents."},{name:"Roster Strength",weight:10,text:"Available player projections or production, with starters weighted 80% and bench depth 20%. Missing player data degrades safely to neutral comparisons."},{name:"Schedule Strength",weight:10,text:"The average points per game of completed opponents. A harder schedule receives a higher score without feeding the power ranking back into itself."}];const preseasonMethods=[{name:"Player projections",text:"The current ESPN scoring-period projection is preferred. Season projection average is the fallback; missing or invalid projections become zero without breaking calculations."},{name:"Value over replacement",text:"Each player is compared with a replacement projection dynamically derived from league size, required starters, and the positions consumed by FLEX or SUPERFLEX slots."},{name:"Optimized lineup",text:"A deterministic optimizer fills every configured starter slot while respecting eligibility and player uniqueness. One player can never fill two positions."},{name:"Starter and depth strength",text:"Position rooms emphasize top starters. Useful reserves contribute with diminishing returns, so roster quantity cannot overpower elite starting talent."},{name:"FLEX handling",text:"Required positional starters are assigned first as part of one legal optimized lineup. FLEX scores use only players assigned to FLEX-like slots, preventing double counting."},{name:"Normalization",text:"Every applicable position group is normalized from 0–100 across this league. Overall score combines group strength scores—not ordinal ranks."}];export default async function Methodology(){const {league}=await getLeague(),weights=league?generatePreseasonWeights(league):{};return <><PageHeader eyebrow="Transparent by design" title="How the rankings work" description="Every score is deterministic, every input is inspectable, and preseason roster quality stays separate from regular-season performance."/><section id="preseason"><div className="eyebrow">Before week one</div><h2>Preseason roster methodology</h2><Card><div style={{padding:24}}><h3>Effective weights for this league</h3><div style={{display:"grid",gap:12}}>{Object.entries(weights).map(([group,weight])=><ScoreBar key={group} label={group} value={weight}/>)}</div><p className="muted">Displayed values are percentages and total {Object.values(weights).reduce((a,b)=>a+b,0).toFixed(0)}%. They adapt to configured QB, RB, WR, TE, FLEX/SUPERFLEX, bench, kicker, and defense demand.</p></div></Card><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:16,marginTop:20}}>{preseasonMethods.map(m=><Card key={m.name}><div style={{padding:20}}><h3>{m.name}</h3><p className="muted" style={{lineHeight:1.65}}>{m.text}</p></div></Card>)}</div></section><section style={{marginTop:50}}><div className="eyebrow">After games begin</div><h2>Regular-season power methodology</h2><Card><div style={{padding:24}}><div className="eyebrow">Power score formula</div><h3>Six signals, one 0–100 score</h3><div style={{display:"grid",gap:14,marginTop:20}}>{metrics.map(m=><ScoreBar key={m.name} label={m.name.replace(" Strength","")} value={m.weight/30*100}/>)}</div><p className="muted">Weights: 30% scoring · 20% recent form · 15% record · 15% expected wins · 10% roster · 10% schedule.</p></div></Card><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:16,marginTop:20}}>{metrics.map(m=><Card key={m.name}><div style={{padding:20}}><div className="eyebrow">{m.weight}% weight</div><h3>{m.name}</h3><p className="muted" style={{lineHeight:1.65}}>{m.text}</p></div></Card>)}</div></section><section style={{marginTop:40,maxWidth:800}}><div className="eyebrow">Reading the numbers</div><h2>Normalization, luck, and ties</h2><p className="muted" style={{lineHeight:1.7}}>Each raw component is min–max normalized across the current league: the strongest value becomes 100 and the weakest becomes 0. When every team has the same value, all receive a neutral 50. Scores are clamped so missing or unusual data can never produce NaN or Infinity.</p><p className="muted" style={{lineHeight:1.7}}>Luck is actual wins (plus half of ties) minus expected wins. Positive luck means the schedule produced more wins than all-play performance predicts; negative luck means a team has performed better than its record.</p></section></>}
