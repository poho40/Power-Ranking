import type { League } from "@/lib/domain";import { applicableGroups,rosterConfig } from "./config";import type { PreseasonGroup } from "./types";
export function generatePreseasonWeights(league:League):Partial<Record<PreseasonGroup,number>>{const c=rosterConfig(league),raw:Partial<Record<PreseasonGroup,number>>={},flex=(c.FLEX??0)+(c["RB/WR"]??0)+(c["WR/TE"]??0),superflex=c.SUPERFLEX??0;if((c.QB??0)>0||superflex)raw.QB=15*Math.max(1,c.QB??0)+10*superflex;if((c.RB??0)>0||flex||superflex)raw.RB=25*Math.max(.5,(c.RB??0)/2);if((c.WR??0)>0||flex||superflex)raw.WR=25*Math.max(.5,(c.WR??0)/2);if((c.TE??0)>0||flex||superflex)raw.TE=12*Math.max(.5,c.TE??0);if(flex+superflex>0)raw.FLEX=13*flex+18*superflex;raw.Bench=5*Math.max(.6,Math.min(1.8,(c.BE??league.teams[0]?.roster.length??5)/7));if((c.K??0)>0)raw.K=2*(c.K??0);if((c["D/ST"]??0)>0)raw["D/ST"]=3*(c["D/ST"]??0);const groups=applicableGroups(league),total=groups.reduce((s,g)=>s+(raw[g]??0),0)||1;const weights:Partial<Record<PreseasonGroup,number>>={};let used=0;groups.forEach((g,i)=>{const value=i===groups.length-1?100-used:Number(((raw[g]??0)/total*100).toFixed(2));weights[g]=value;used+=value});return weights;}
