import type { League } from "@/lib/domain";import type { PreseasonAward,PreseasonGroup,PreseasonTeamRanking } from "./types";
export function preseasonAwards(rankings:PreseasonTeamRanking[],groups:PreseasonGroup[],league:League):PreseasonAward[]{const name=(id:string)=>league.teams.find(t=>t.id===id)?.name??"Unknown",awards:PreseasonAward[]=[];const overall=rankings[0];if(overall)awards.push({id:"overall",label:"Best Overall Roster",teamId:overall.teamId,detail:`${name(overall.teamId)} leads with a ${overall.overallScore.toFixed(1)} roster score.`});for(const group of groups.filter(g=>g!=="Bench")){const best=rankings.toSorted((a,b)=>(a.positionGroups[group]?.rank??99)-(b.positionGroups[group]?.rank??99))[0];if(best?.positionGroups[group])awards.push({id:group,label:`Best ${group} ${group==="FLEX"?"Group":"Room"}`,teamId:best.teamId,detail:`#1 ${group} score: ${best.positionGroups[group]!.score.toFixed(1)}.`})}const deepest=rankings.toSorted((a,b)=>(a.positionGroups.Bench?.rank??99)-(b.positionGroups.Bench?.rank??99))[0],thinnest=rankings.toSorted((a,b)=>(b.positionGroups.Bench?.score??0)-(a.positionGroups.Bench?.score??0))[0];if(deepest)awards.push({id:"deep",label:"Deepest Bench",teamId:deepest.teamId,detail:`The league's strongest reserve value belongs to ${name(deepest.teamId)}.`});if(thinnest)awards.push({id:"thin",label:"Thinnest Roster",teamId:thinnest.teamId,detail:`${name(thinnest.teamId)} has the least value beyond its optimized starters.`});return awards;}
