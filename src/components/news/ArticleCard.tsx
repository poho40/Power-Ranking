import Link from "next/link";import type { NewsArticle } from "@/lib/news";import { Card } from "@/components/ui/Card";
export function ArticleCard({article}:{article:NewsArticle}){return <Card className="hover-lift"><article style={{padding:22}}><div className="eyebrow">{article.heroLabel??`${article.season} · Week ${article.week}`}</div><h2 style={{fontSize:26,letterSpacing:"-.025em",marginBottom:10}}><Link className="link" href={`/news/${article.slug}`}>{article.title}</Link></h2><p className="muted" style={{lineHeight:1.65}}>{article.summary}</p>{article.tags?.length?<div style={{display:"flex",flexWrap:"wrap",gap:7,margin:"15px 0"}}>{article.tags.map(tag=><span key={tag} style={{border:"1px solid var(--line)",borderRadius:99,padding:"4px 8px",fontSize:11}}>{tag}</span>)}</div>:null}<div style={{display:"flex",justifyContent:"space-between",gap:12,fontSize:13}}><time dateTime={article.publishedAt}>{new Intl.DateTimeFormat("en-US",{dateStyle:"medium",timeZone:"UTC"}).format(new Date(`${article.publishedAt}T00:00:00Z`))}</time><Link className="link" href={`/news/${article.slug}`}>Read article →</Link></div></article></Card>}
