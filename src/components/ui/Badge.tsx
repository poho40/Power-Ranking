export function Badge({children,tone="default"}:{children:React.ReactNode;tone?:"default"|"up"|"down"}){const c=tone==="up"?"var(--accent)":tone==="down"?"var(--red)":"var(--muted)";return <span style={{display:"inline-flex",padding:"4px 8px",border:"1px solid var(--line)",borderRadius:999,color:c,fontSize:12,fontWeight:750}}>{children}</span>}
export function Movement({change}:{change?:number}){if(change===undefined||change===0)return <Badge>— <span className="sr-only">No rank change</span></Badge>;return <Badge tone={change>0?"up":"down"}>{change>0?"▲":"▼"} {Math.abs(change)} <span className="sr-only">{change>0?"places up":"places down"}</span></Badge>}
