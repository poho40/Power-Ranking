export function ScoreBar({label,value}:{label:string;value:number}){const safe=Math.max(0,Math.min(100,Number.isFinite(value)?value:50));return <div style={{display:"grid",gridTemplateColumns:"105px 1fr 38px",alignItems:"center",gap:10,fontSize:13}}><span className="muted">{label}</span><div style={{height:7,background:"#252e3a",borderRadius:10,overflow:"hidden"}}><div style={{width:`${safe}%`,height:"100%",background:"var(--accent)",borderRadius:10}} /></div><strong className="metric">{safe.toFixed(0)}</strong></div>}
